version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - .env:/app/.env
    environment:
      - DATABASE_URL=sqlite:///./data/agent_api_proxy.db
    restart: unless-stopped
